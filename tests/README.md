# Tests Catalog

Формат записей: `file / when to run / covers`

- `tests/run.ps1 / after code changes or before commit/push when a centralized test batch is needed / runs named suites: `dev` for the fast default package set used by `scripts/dev_check.ps1`, `regression` for tracked regression tests only, `full` for core workspace unit/regression/UI-contract/browser-parity packages, and explicit `cloud-live` for networked Cloudflare Worker smoke checks`
- `tests/cloud_auth_smoke.ps1 / manually after changing Worker auth endpoints, D1 auth/schema/tag migrations, Google OAuth start/poll flow, OAuth UI language rendering, SVG captcha tile rendering/rejection/refresh/zoom markup, browser-verification challenge markup, quota endpoints, app/tag catalog, observation trust/source/publisher-key handling, or when diagnosing live auth errors / creates a unique client identifier against `NETSTITCH__CLOUD_BASE_URL` or the default `netstitch-sync` Worker, checks `/v1/health`, `/v1/auth/google/start`, the `5 за 15 минут` OAuth start limiter, `/v1/auth/session/poll`, `/v1/client/quota`, `/v1/apps`, `/v1/tags` ownership metadata, verifies direct tag creation/rename are unavailable outside upload, verifies the OAuth page exposes captcha plus state-bound browser-verification proof fields without exposing Google URL, and verifies tag deletion plus `/v1/users/me/apps` reject anonymous requests; this is a live network/D1 smoke and is intentionally not part of `dev` or `full``
- `tests/web_access_smoke.ps1 / manually after changing local browser shell, HTTPS startup, web-key authorization, portable launch layout, or embedded watcher startup / from the Windows host starts Windows and/or Linux portable apps one at a time with an isolated temp data dir and `NETSTITCH__WEB_UI=1`, verifies `https://127.0.0.1:46473/health`, root browser shell, unauthorized browser snapshot rejection, desktop-header `/v1/web-url`, `/v1/web-auth` POST with the URL fragment key, and authenticated browser `/v1/snapshot`; use `-Target windows`, `-Target linux`, or default `both`; for an elevated Windows app already started by the user, use `-Target windows -AttachExisting``
- `src/netstitch-cloud/src/lib.rs + src/netstitch-shared/src/cloud.rs (unit tests) / after changing community cloud sync helpers, client identifier derivation, quota behavior, retention policy, verified-app upload policy, stable app identity hashing, cloud app-list/status scopes, request JWT signing, nickname/source ownership contract, upload batch limits, or cloud observation DTO usage / covers explicit user-action gating for cloud requests, local quota mirror spending including unlimited `limit_count = 0`, overlay bootstrap operation scopes, one-year observation expiry helpers, ES256 client key generation and upload JWT signing, stable privacy-preserving client identifier derivation, empty identifier input rejection, lower-case stable app identity hashing, signed-vs-unsigned identity separation, and upload request validation for verified app observations including max batch size`
- `src/netstitch-ui/src/secure_store.rs (unit tests) / after changing local cloud credential storage / covers Windows DPAPI roundtrip and verifies protected local auth/session/key blobs do not contain plaintext secrets`
- `src/netstitch-ui/src/cloud_sync.rs (unit tests) / after changing cloud download pagination, app catalog filtering, quota display, author signatures, tag validation, or downloaded-row staging behavior / covers the real download loop against a local HTTP server with 500+102 paged rows, raw page-size offset advancement, underreported `total` values, duplicate-page stop protection, no automatic row selection after download, cloud app id generation, upper-case English ASCII tag normalization without spaces, and local cloud sync helper contracts`
- `scripts/check_cloud_worker.ps1 / after changing src/netstitch-cloud-worker/src/worker.js or D1 migrations / syntax-checks the Cloudflare Worker entrypoint without deploy, network, live D1 ids, or secrets; tag changes also require reviewing `0015_observation_tags.sql`, `0016_uppercase_tags.sql`, account/row limits, expiry cleanup and substring filters before live deploy`
- `tests/netstitch-regression/tests/package_app_docs.rs / after changing Cloud Worker observation pagination or tag response mapping / verifies that paged logical endpoint rows include the deduplicated non-expired `tags` array used by both cloud-import staging UIs`
- `tests/netstitch-regression/tests/language_files.rs / after changing shipped UI language files or i18n fallback behavior / validates that all shipped language ini files expose the same string keys as en-en, contain non-empty values, and preserve the English fallback contract for missing localized keys`
- `tests/netstitch-regression/tests/package_app_docs.rs / after changing portable connector-app packaging, shipped connector-app README naming rules, runtime connector examples, external integration split boundaries, module SDK docs, SDK example archives, release archive layout, GitHub portable release workflow, or Cloud Worker observation pagination / validates that resources/connectors/apps ships non-empty README_RU.txt and README_EN.txt variants for the portable apps folder, ships `*.app` instead of `*.toml`, does not reintroduce user-specific runtime connector examples or product hardcode into tracked source/resources/tooling, verifies that core tracks only the generic integration host and keeps bundled modules opt-in, checks Module SDK docs/examples including unchanged native C/JSON ABI wording, event subscriptions, background-task no-autostart, log/dialog commands, reserved module names, predictable UI layout defaults, module footer/back behavior, `table_columns`, two identical UI Entity Showcase examples on Rust/C++, ready Windows/Linux example archives, and manual-only cloud/CSV actions, checks that Windows portable packaging preserves user `.app/.svg/.png` connector files while removing legacy `.toml`, checks an existing Windows release zip for clean portable `apps/` contents without legacy presets, runtime `manual_*.png` icon cache, or runtime SQLite databases, verifies release staging cleanup for Windows/Linux, verifies release-branch workflow build-and-publish behavior, and keeps Cloud Worker observation download on real `limit`/`offset` pagination instead of a first-page-only query`
- `tests/netstitch-regression/tests/package_app_docs.rs / after changing cloud export preview matching or author tag grouping / verifies the authenticated Worker match route, exact author/endpoint key, own-tag filtering, bounded batch size, and `tag_groups` response used by desktop/browser to avoid counting already published endpoint/tag pairs as New data`
- `scripts/package_linux_deb.sh + tests/netstitch-regression/tests/package_app_docs.rs / after changing Linux installable packaging / verifies the Debian package script builds from Linux portable staging, ships `/opt/netstitch`, `/usr/bin/netstitch`, a desktop launcher, required native `.so` files, `shin0by` app icon, Debian/Ubuntu runtime dependency alternatives plus `iputils-ping`/`traceroute`/`dnsutils`, strips shipped module `data/`, and routes writable app/module runtime state to `~/.local/share/netstitch` through env vars`
- `resources/linux/install-desktop-launcher.sh + tests/netstitch-regression/tests/package_app_docs.rs / after changing Linux portable fallback install behavior / verifies user-local copy mode, launcher creation, `ldd` missing-library diagnostics, and package-name hints for Debian/Ubuntu, Debian 13, Fedora, Arch, and openSUSE`
- `src/netstitch-shared/src/domain_detection.rs (unit tests) / after changing advanced-monitoring verified-domain extraction rules / covers payload-level extraction from HTTP `Host`, TCP TLS ClientHello SNI, QUIC Initial SNI on UDP/443, and rejects IP-only/UDP DNS guesses as non-verified domains`
- `src/netstitch-ui/src/tray.rs (unit tests) / after changing tray menu ids or tray action routing / covers every tray menu item mapping (show/hide window, hide when minimized, start/stop scanning, exit) through stable menu ids, not localized labels`
- `src/netstitch-ui/src/main.rs (unit tests) / after changing desktop window bootstrap, single-instance startup behavior, or size policy / covers the `1350x840` minimum inner window size contract, verifies the initial size is not smaller than the minimum, and checks that a second desktop launch activates/restores the existing NetStitch window without showing a message box`
- `src/netstitch-ui/src/ui_entities.rs (unit tests) / after changing UI ids, data-ui entities, data-ui actions, or dynamic row ids / covers stable non-localized selectors for UI automation, including bulk tracked-app toggling, per-app delete actions, integration download progress/cancel actions, and delete confirmation modal actions instead of first-row debug selectors`
- `src/netstitch-ui/src/app_watcher.rs (unit tests) / after changing UI-to-embedded-watcher mapping, startup behavior, runtime readiness status, monitor status rendering, connector display-name/icon mapping, tray setting mapping, or timestamp conversion / covers live snapshot mapping helpers, non-blocking cold-start, embedded runtime readiness, persisted hide-to-tray setting propagation, Chrome display-name capitalization, and stable status/error text expectations for real runtime mode`
- `src/netstitch-ui/src/app_watcher.rs (embedded runtime lifecycle unit tests) / after changing embedded watcher startup/shutdown behavior / covers lifecycle cleanup used when UI exits and verifies the native-only watcher lifecycle`
- `src/netstitch-ui/src/app.rs (unit tests) / after changing footer status, footer message behavior, panel footers, module UI footer/back behavior, module UI layout defaults, standard module window layout, dynamic module action enabled/disabled state, universal module layout rows/spacers, cloud import/export panel footers, tag picker, observation filtering rules, monitoring column visibility, observation row actions, ignored-address delete flow, information overlay, update-check UI, domain-capture admin prompt, IP enrichment display/filtering, icon source handling, tooltip entities, render-timer behavior, input commit behavior, clear-button empty-field disabled state, cloud author/my-publications behavior, or embedded UI icon assets / covers footer Apps label contract, Monitoring panel footer Rows/displayed/selected counters, watcher/tool/web-server status entity/indicator/tooltip contract, footer Message backed by `system_events`, connector/app-availability diagnostics, viewport-safe tooltip alignment, web-server URL clipboard/message contract, module UI predictable defaults, module root fullscreen/viewport layout for the standard module window, `set_ui_values` action state overrides, `layout_row` content-sized children and `spacer` row distribution, host-owned module `Назад` footer navigation, cloud import/export footer progress placement with 500-row chunks, tag picker use of standard compact modal header/body/footer plus separate search/add and chip-selection state with one assign/remove action, cloud export exposing only local selected-row tag cleanup, cloud import footer Rows/selected counters, locally filtered downloaded cloud rows with author tags first, Tag directly after App in the staging table, staging visibility switches sharing persisted `ui.monitoring.show_tags` and `ui.monitoring.show_connection_count`, cloud add-to-monitoring requiring explicit staging selection, clearing stale cloud staging rows before a new download, disabling cloud import actions while download is loading, cloud `Мои публикации` author filter and Authors column display from session-owned `author_signature`, failed-attempt filter behavior, domain wildcard filtering, domain/owner/range-aware observation filtering, public/non-public switch filtering and `ui.monitoring.public_ip` persistence, direct persisted Tag and Connection/Count visibility through typed `app_settings`, explicit `Enter`/`change` text-input commits without `oninput`, dimmed clear buttons for empty bound fields, domain-capture switch refusal without elevation, per-row monitoring delete modal-confirm action, Monitoring Delete All/Delete Selected id collection and backend deleted-count reporting, ignored-address modal-confirm with localhost/local-IP notes, information dialog branding/panel/link/icon/update-button contract, app version/revision comparison for GitHub release hints, observation rows plus SVG/bitmap icon data URI generation, icon render-path caching, and square close-button icon data URI generation`
- `src/netstitch-ui/src/app.rs + src/netstitch-ui/src/theme.rs + src/netstitch-watcher/src/lib.rs (unit tests) / after changing Monitoring row diagnostics / verifies the `...` action precedes delete, selected-row IP/domain defaults remain per-tab, desktop diagnostic fields keep the non-rendering draft contract, desktop/browser panels use a compact full-width inline label/value context row plus equal control/result columns with an unlabeled output on the right, panel entities and tabs stay in parity, the local API uses the shared native-tool lock, and diagnostic target/output are excluded from system-event payloads`
- `src/netstitch-ui/src/app.rs (CSV unit tests) / after changing CSV import/export, monitoring table columns, app identity columns, or signature provenance / covers selected-row CSV export with the same visible Monitoring columns plus app identifiers/signature fields and CSV parsing into `MonitoringCsvImportRequestDto``
- `src/netstitch-ui/src/app.rs + src/netstitch-ui/src/theme.rs + src/netstitch-watcher/src/lib.rs (unit tests) / after changing cloud upload nickname status or cloud export Tag cell layout / covers the always-rendered fixed 16px nickname status slot, identical desktop/browser tag widths calculated from the 188px maximum uppercase field plus X/chrome slots, zero tag-cell side padding, equal 100%-width closed/open dropdown surfaces, disabled-and-closed tag triggers while cloud data is refreshing, compact first-tag plus-count summaries, blue closed summaries when removable local tags exist, and local-first tag-chip rendering with an individual X action`
- `src/netstitch-ui/src/mock.rs (unit tests) / after changing mock tracked apps, delete behavior, or mock-monitor seed behavior / covers connector-style mock apps where detected known apps start disabled and deleting a tracked app removes it from active tracking while preserving related observations`
- `src/netstitch-core/src/lib.rs (unit tests) / after changing SQLite bootstrap, app settings, effective tracked-app overlays, ignored address rules, endpoint probe diagnostics, IP enrichment cache, tracked-app sorting, tracked-app delete, observed endpoint delete/bulk delete, connector/manual icon cache, connector tracked-app seeding logic, integration discovery, profile export enrichment/statistics flow, system event logging, or runtime snapshot diagnostics / covers disabled insert of discovered connector apps, preservation of existing tracked rows, connector identity repair for rows that were accidentally manualized by previous toggle flows, enabled-by-id toggle preserving connector display/icon/order, distinct runtime connector rows for different connector ids even when executable paths match, runtime `.app` discovery from a user folder while ignoring adjacent `.toml` files and seeding the tracked app disabled, archiving tracked apps without deleting their observations, deletion of a single observed endpoint, batched deletion of many observed endpoints, SQLite `app_settings` persistence for `ui.language`, `ui.enable_all_overlay`, `ui.hide_when_minimized`, `ui.modules.order`, `web.localhost_enabled`, optional `ip_enrichment.enabled`, `system_events` severity/repeat_count/9999-row rotation, runtime integration module diagnostics in snapshot, core SQLite schema allowlist that excludes external module runtime tables/settings, default localhost plus local-machine IP ignore rules, ordered 8x IPv4/8x IPv6 local-IP route probes with `10s` timeout, filtering of Teredo/6to4/special local-IP seeds, cached DNS reachability probe status, cached endpoint-level verified-domain/whois enrichment attachment to snapshot/profile-export rows, trusted CSV domain import into endpoint-level cache, 1000-row WHOIS range cache rotation, effective enable-all overlay without rewriting individual tracked-app flags, embedded `.svg` icon cache, no default manual app seeding for fresh SQLite databases, manual executable icon fallback, retrying missing/broken manual icon cache generation, rejection of fully-transparent manual PNG cache files, full-path manual icon cache names for duplicate exe filenames, manual-newest/connector-alphabetical ordering, connector count and unavailable tracked-app diagnostics, and cached app availability path checks for UI refreshes`
- `src/netstitch-core/src/lib.rs (CSV import unit tests) / after changing monitoring import persistence / covers cumulative CSV import that never clears existing monitoring rows, imports more than one cloud page worth of rows without truncating at 500, skips existing endpoints by `remote IP + port + protocol` instead of reimporting them, restores app identifiers, preserves imported signature provenance without replacing it with local app evidence, and keeps `CloudDownload` tags as separate cloud provenance that local tag cleanup does not remove`
- `integrations/host/src/lib.rs (unit tests) / after changing integration host manifest discovery, platform library path selection, module status diagnostics, module storage routing, event subscriptions, background-task host control, module dialogs, module event logging, or reserved module names / covers mapping an installed module manifest to portable `integrations/<module>/data` storage, keeping bad manifests from blocking valid modules, reporting module load errors to runtime diagnostics, selecting native shared library paths from manifest `library_paths`, rejecting `core`/`system` module ids case-insensitively, routing module log events with `source = <module_id>`, and keeping background tasks stopped until explicit `start_background`
- `integrations/<module>/crates/<module>/src/lib.rs (module-owned unit tests) / after changing a module's own settings, provider/download state, or export UI state persistence / covers writing module runtime settings to `integrations/<module>/data/module.sqlite3` and not creating/mutating core `storage/netstitch.sqlite3`; these tests are owned by each module contour and are not hardcoded in the core runner`
- `src/netstitch-tool/src/lib.rs (unit tests) / after changing native network tool ABI, endpoint reachability checks, ping/trace/DNS diagnostics, or DNS/WHOIS library behavior / covers native ABI health, Team Cymru WHOIS prefix/owner parsing, protocol-aware endpoint target parsing/status output for `host:port` targets, validated direct-argument diagnostic commands, custom DNS scope, bounded streaming output callbacks before process completion, rejection of non-canonical operation names, and DNS UDP probe behavior before writing SQLite cache`
- `src/netstitch-connectors/src/lib.rs (unit tests) / after changing connector discovery helpers, embedded connector icons, shipped `resources/connectors/apps/*.app`, or runtime connector manifest parsing / covers first-existing, versioned-child, Windows App Paths value parsing, generic registry string path values, root-path discovery over drive/game roots, Store/MSIX package folder discovery, macOS app bundle lookup, Linux desktop-entry parsing, runtime `.app` parser contract, loading every shipped `.app` manifest as a runtime connector, distinct connector ids for the same executable path, runtime icon-key mapping, embedded known-app icon bytes, normalized small-slot SVG viewports, and wildcard path matching used by known-app modules`
- `src/netstitch-watcher/src/monitor.rs (unit tests) / after changing TCP-state classification, WinDivert FLOW mapping, or packet-level verified-domain capture / covers mapping of TCP table states and UDP/QUIC FLOW events into failure/success connection-state buckets, recent TCP owner keys for delayed domain matching, outbound IPv4 TCP payload parsing for HTTP Host on arbitrary ports, UDP/443 QUIC Initial SNI parsing, fragmented Host accumulation, packet-domain ingest diagnostics, and rejection of packets without verified domain data`
- `src/netstitch-watcher/src/lib.rs (unit tests) / after changing embedded watcher runtime routes, network diagnostics, cloud-signature backfill action, monitoring control, or filesystem routes / covers embedded runtime startup/shutdown, local routes, tracked app mutations, monitoring control, bounded short-lived Ping/Trace/DNS jobs whose output is readable before completion, generic export actions, cloud-signature backfill action behavior, and native-only entry semantics`
- `src/netstitch-watcher/src/lib.rs (module UI action unit tests) / after changing module API, UI action routing, host commands, module host events, background tasks, standard dialogs, or desktop/browser module action payloads / covers `/v1/integrations/ui-action`, selected/displayed monitoring row payloads, host-event payloads for controls/tables/filters/monitoring, `start_background`/`stop_background`, `log_event`, `show_dialog` result logging, the four allowed severity values, and the whitelist that prevents modules from automating cloud or CSV actions`
- `src/netstitch-shared/src/ipc.rs (unit tests) / after changing shared runtime DTOs or retiring obsolete process-transport DTOs / covers canonical serialization contracts still used across embedded runtime/browser/integration boundaries`
- `src/netstitch-watcher/src/lib.rs (browser UI unit tests) / after changing browser shell markup, local web-access behavior, web language selection, browser icon delivery, fixed panel layout, footer status/message entities, module UI footer/back behavior, module UI layout defaults, cloud import/export footer progress, tooltip entities, IP enrichment display/filtering, ignored-address delete flow, integration overlay progress, endpoint probe request format, browser filter synchronization, tracked-app toggle routing, runtime-host file picker routes, clear-button disabled-state synchronization, or shared desktop/web visual contracts / covers duplicated desktop panels, unified `Tracked apps`-style panel headers with `title-with-help` and help `?`, browser actions, app icon routes, close-button SVG assets, `ui.language` setting writes, root `/` unique-port path contract without `/web` or app-name path, HTTPS web-key URL fragment, `/v1/web-auth` POST auth, web-auth cookie API gate, same-origin/CSRF/security-header contract, `/v1/web-url` footer/clipboard contract, absence of `file://` image loading, plus/add-exe icon-button parity, runtime-host executable/profile/CSV file picker contract, compact ignored-address list rows with domain fields and modal-confirm delete, monitoring Domain columns without IP-public table column, public/non-public switch filter, enrichment tooltips, cloud import/export footer progress targets with 500-row chunks, cloud privacy column `Private`/`Прив.` with true/false values, browser footer messages loaded from `system_events`, browser cloud observation proxy paging against a local HTTP server with 500+102 rows, browser cloud add-to-monitoring requiring explicit staging selection, integration modal status rows, module UI predictable defaults, host-owned module `Back` footer navigation, `/v1/integrations/download-progress`, profile export additional-settings overlay layout parity, real integration stage labels and segmented `progress-bar` contract, footer Apps region, runtime/tool/web-server indicators, footer message panel, protocol-aware endpoint probe snapshot params, custom language dropdown anchoring, viewport-safe tooltip behavior, fixed desktop/browser panel placement, desktop-vs-browser palette/control/icon/style parity checks, section-delta browser refresh including filters without dropping canonical rows, browser toggle route that updates enabled state by id without re-adding connector apps, Chrome display-name capitalization, content-width path fields, a main-window element map that compares header controls, primary panels, monitoring table regions, footer, and overlay dialogs between desktop and browser shells, plus ordered header/panel contracts, clear-button containment and empty-field disabled synchronization, help-icon geometry, footer/table containment, canonical region shell classes/ids, canonical header filter blocks, and obsolete browser-only token rejection for the browser shell`
- `scripts/compare_main_window_visual.ps1 (manual visual smoke) / after changing UI layout, typography, icon sizing, dropdowns, tooltips, panel spacing, or footer/header behavior / captures desktop and browser-shell screenshots for the main window, writes a browser DOM dump, crops `header`, `top-panels`, `monitoring`, and `footer` regions, and saves diff PNG plus `summary.json` artifacts under `temp\visual-debug\main-window\...` so desktop-vs-browser drift can be reviewed and fixed from concrete evidence`
- `config/signing.example.toml + docs/SIGNING_RU.md (manual review) / after changing signing data requirements or release signing policy / covers safe tracked signing metadata, required user-provided fields, local secret boundaries, self-signed test certificate as test-signed release status, non-blocking `release` promotion/publication, and the rule that NetStitch signs project-owned app/native-library files but does not re-sign WinDivert runtime`
- `scripts/sign_windows.ps1 (manual smoke) / after changing local signing workflow / covers reading ignored signing config, creating or reusing a CurrentUser self-signed code-signing certificate, signing existing project-owned `.exe` and `.dll` artifacts, and keeping trust-store mutation behind explicit `-EnsureTrustedForCurrentUser`
- `src/netstitch-ui/build.rs + native module build metadata (manual metadata smoke) / after changing Windows executable metadata, native library metadata, or signing policy / covers VersionInfo fields for `NetStitch.exe` and available metadata/signing checks for project-owned native libraries`
